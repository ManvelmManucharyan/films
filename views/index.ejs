<h2>Recently Added</h2>
<div>
    <% films.forEach(film => { %>
        <a href="/films/<%= film.id %> ">
            <div><%= film.title %></div>
            <img height="150" width="100" src="<%=film.coverImagePath%>">
        </a>
    <% }) %>
</div>